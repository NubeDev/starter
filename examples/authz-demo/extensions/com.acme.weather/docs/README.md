# com.acme.weather

Demo extension that contributes two REST endpoints — one read, one
read/write — used to show per-user authorization gating from the
host. Both are built-in (in-process) handlers registered by the
demo's `server.rs`.
