## Done

- Added `crates/starter-ui-ir` as a workspace member; ported `Component`, `Bindable`, `Bindings`/`BindingSpec`, `Concurrency`, `ResolveIssue`, and the chart source / kind / agg types verbatim from `rubix-contracts/ui-ir`.
- Set `IR_VERSION = 5` and ported `ComponentTree`.
- Replaced the Rubix proc-macro `#[derive(Bindable)]` with a hand-written `impl Bindable for Component` so the crate has zero proc-macro / I/O deps per R1 (deps are only serde, serde_json, schemars, thiserror, tracing).
- Applied divergence D1: `ActionResponse::Diagnostics { items: Vec<Diagnostic> }` is the only error/warning/info variant; `form_errors` is rejected at deserialise. `Diagnostic { severity, code, message, field?, details? }` lives in `src/diagnostic.rs`.
- Added `pub mod schema` + `bin/emit_schema` and committed deterministic JSON Schema artifacts under `crates/starter-ui-ir/schema/` for the component tree, action request, and action response — what `starter-ai-builder-prompt` (Phase 1 dependency in the job model) consumes.
- Tests: 49 unit tests + 10 integration tests passing (`bindable_dispatch.rs`, `schema_artifact.rs`, `wire_compat.rs`). Covers serde round-trip, V5 chart dual-field tolerance, additive Divider variant, deterministic schema emission, and the D1 `form_errors` rejection.
- Verified R1 via `cargo tree -p starter-ui-ir --edges normal` — no axum / reqwest / hyper / tokio / tower / h2 / http-body edges.
- Committed as `Phase 1 -- starter-ui-ir port` (c255f97).

## Next

- (none) — Stage 4 picks up in a fresh session.

## What you need to know

- D1 and D3 are already documented in `DOCS/frontend/sdui/DIVERGENCE.md` from a prior commit; this stage didn't need to edit the file, contrary to the stage prompt's phrasing.
- `IR_VERSION = 5` is what the Rubix port shipped with. Bumping it requires the SCOPE R2 process (allowlist diff, deprecation window).
- The JSON Schema artifacts must be regenerated by `cargo run -p starter-ui-ir --bin emit_schema` whenever the IR changes; `tests/schema_artifact.rs` will fail loudly if a contributor edits the IR without regenerating.
- No CI gate file was added for the R1 `cargo tree` denylist — only manual verification done. A later stage / CI stage will likely add the gate.
- `[lints] workspace = true` was omitted from `Cargo.toml` because the workspace root has no `[workspace.lints]` table; matches existing crates.

## Open questions

- Should the `ActionRequest` / `ActionResponse` wire types live in `starter-ui-ir` (as done here) or wait for `starter-ui-bindings` / `starter-sdui-routes`? They were included here because D1 explicitly applies to action response in this stage and `starter-ai-builder-prompt` benefits from the schema artifact for the response shape. A later stage can move them if the scope owner prefers.
- The original Rubix `trybuild` compile-fail fixtures for `#[derive(Bindable)]` were dropped (no derive); equivalent structural coverage now lives in the hand-written dispatch tests. Confirm that's acceptable.
