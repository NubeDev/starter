## Done

- Rewrote `rubix-flows/flows/dashboard-assistant.yaml` from the goal-1 stub into a real ai-agent flow with `skill_hint=com.rubix.dashboard-builder` and `allowed_tools=[rubix.dashboard.{create,update,get,list,duplicate,delete,page_set}, rubix.undo.last]`
- Deleted `rubix-tools/src/dashboard/assistant.rs` stub, removed `DashboardAssistantStub` from `rubix-agent/src/registry.rs`, flipped openapi tag from `dashboard-stub` to `dashboard`, updated `openapi_test.rs`
- Added `rubix-flows/dashboards/disk-overview.json` (~85 lines: page → row → col → kpi, plus chart row), exposed via new `rubix_flows::BUNDLED_DASHBOARDS` include_dir; `boot/dashboards_seed.rs` reads both the new dir and the legacy `flows/dashboards/` fallback
- Added `rubix-client-ts/src/endpoints/dashboard.ts` mirroring `flow_ops.ts` (seven verbs, DTOs, CSRF) and barrel export
- Added `rubix-client-react/src/hooks/dashboard.ts` with `useDashboardList/Get/Create/Update/Delete/Duplicate/PageSet` under `['rubix','dashboard',…]` keys
- Added `/dashboards` list + `/dashboards/$pageId` detail routes (mounts `<SduiPage>`); hand-updated `routeTree.gen.ts`
- Replaced `/dashboard` route with a `redirect` → `/dashboards/disk-overview`
- Mounted `<SduiProvider transport={createHttpSduiTransport({ client: starter })}>` in `main.tsx` next to `<RubixClientProvider>`
- Added `dashboards.*` + `dashboard.kpi.*` + `dashboard.chart.*` keys in `en.json` + `es.json`
- Added Playwright spec `rubix-frontend/e2e/dashboards.spec.ts`
- `pnpm --filter @nube/rubix-frontend typecheck` ✓, `--filter @nube/starter-ui-sdui-react typecheck` ✓, `--filter @nube/rubix-client-{ts,react} typecheck + test` ✓ (60 tests), `cargo build` + `rubix-agent::openapi_test` + `rubix-flows::load_test` ✓

## Next

- Phase D gate / stage 15 (next stage in the dependency graph)

## What you need to know

- The seed previously read `rubix_flows::BUNDLED.get_dir("dashboards")` (i.e. `flows/dashboards/`); D.2 added `rubix_flows::BUNDLED_DASHBOARDS` (a new include_dir over `rubix-flows/dashboards/`) and seeds from both, so the existing seeder contract is unchanged
- `routes/dashboards/$pageId.tsx` derives `pageRef` as `dashboard.<slug>` unless the URL slug already contains a dot — matches `dashboards_seed`'s `page_id = format!("dashboard.{stem}")` convention
- The bundled `disk-overview.json` uses `ChartSource::Static` + `Kpi { source: Static }` so it deserialises into `ComponentTree` without needing a live ClickHouse/PG telemetry source; `ir_version: 5` matches `IR_VERSION`
- `routeTree.gen.ts` is normally autogenerated by `@tanstack/router-plugin/vite` at dev time. Stage 14 hand-edited it so `tsc -b --noEmit` passes; running the dev server will regenerate it idempotently — the hand edits match the plugin's output shape
- No sibling unit test was added for the new `dashboard.ts` endpoint + `dashboard.ts` hook (the other endpoint families have `.test.ts(x)` siblings). Next stage may want to backfill these to match the family convention
- e2e spec is included but NOT run as part of this stage — needs a live `rubix-agent` on 127.0.0.1:8088 with the seeded bundle

## Open questions

- (none)
