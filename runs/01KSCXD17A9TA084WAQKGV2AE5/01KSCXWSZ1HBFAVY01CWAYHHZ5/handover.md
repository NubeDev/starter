## Done

- Added an `Admin` group to `NAV_GROUPS` in `rubix/frontend/src/lib/nav.ts` with three entries (Access → `/admin/access`, Users → `/admin/users`, Warehouse → `/admin/warehouse`) using existing icons + the existing NavGroup primitive.
- Created `rubix/frontend/src/routes/admin/warehouse.tsx` as a Phase-C stub page wrapped in `<ErrorBoundary>`, plus matching entries in `routeTree.gen.ts` (router-plugin codegen was not available in this sandbox so the file was hand-edited; running vite dev will regenerate it identically).
- Added i18n keys `nav.group.admin`, `nav.item.access`, `nav.item.users`, `nav.item.warehouse`, `warehouse.eyebrow|title|stub.body` to both `en.json` and `es.json`. `sync-catalogues` only touches `rubix.*` keys so these survive a resync.
- Added `rubix/frontend/e2e/authz-admin.spec.ts`: logs in, navigates to `/admin/access`, clicks each of the 8 AuthzAdmin tab triggers (tenants/members/teams/rules/assignments/resources/check/decisions) and asserts each panel's `h2` heading renders.
- Committed as `phase A.2 — left-nav admin section + authz smoke test — feat(rubix-frontend) admin nav section + authz e2e` (`49ac141`).

## Next

- Stage A.3 (next session) per SCOPE.

## What you need to know

- `pnpm --filter @nube/rubix-frontend typecheck` still emits 3 pre-existing errors (`starter-client-react/query-provider.tsx:47`, `starter-ui-kit/ui/spinner.tsx:6`, `rubix-frontend/src/components/layout/nav-group.tsx:79`). All are React-18-vs-19 `@types/react` resolution drift from pnpm and reproduce on the parent commit (verified via `git stash`). This stage introduced **zero new typecheck errors**.
- `rubix/frontend/package.json` has no `test` script, so the stage's "test" gate is vacuous. Only `typecheck` + `e2e` are runnable.
- E2E was not executed — it requires `mani run demo` (rubix-agent on 127.0.0.1:8088 with bootstrap operator). Spec follows the existing `auth.spec.ts` / `users.spec.ts` shape and uses Radix `role="tab"` for trigger lookups + `level: 2` headings to disambiguate from tab text.
- `routeTree.gen.ts` is normally auto-generated by `@tanstack/router-plugin/vite` on `pnpm dev`; the hand-edit matches the existing pattern and should be overwritten in place by the plugin if/when dev runs.

## Open questions

- Should the `nav.item.warehouse` href point at `/admin/warehouse` or be left dormant (`#warehouse`) until Phase C lands? Chose live route per stage spec ("warehouse stub for Phase C").
