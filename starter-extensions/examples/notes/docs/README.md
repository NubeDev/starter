# notes

A single bundled extension that demonstrates cross-surface contribution: the
same in-memory note store is reachable via MCP, REST, CLI, and a React UI
panel — all wired through the standard `starter-ext-host` loader.
