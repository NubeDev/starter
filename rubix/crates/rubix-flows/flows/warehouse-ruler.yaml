# Bundled rubix flow — see SCOPE Phase plan for which phase wires this.
id: com.rubix.warehouse-ruler
description: |
  warehouse-ruler flow rooted at an ai-agent node. Auto-surfaces as an MCP
  tool via FlowAsTool (no per-flow MCP wiring per SCOPE R7).
trigger: explicit

nodes:
  - id: agent
    kind: ai-agent
    config:
      session_policy: continue
      skill_hint: com.rubix.warehouse-ruler
      cost_cap: 0.50_usd
      allowed_tools:
        # First entry is the zero-arg "primary tool" — see
        # boot/mcp/register.rs::primary_tool_for_root.
        - rubix.warehouse.rule.list
        - rubix.warehouse.rule.write
        - rubix.warehouse.mart.create
        - rubix.warehouse.retention.set
        - rubix.undo.last

links: []
