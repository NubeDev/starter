---
id: com.rubix.example.echo-skill
description: |
  Demonstration skill — echoes the user's input through the
  example tool. Pick this skill only when explicitly testing
  extension contribution.
allowed_tools:
  - com.rubix.example.echo
trust: quarantined
---

# Example echo skill

Reference content. Extension-shipped skills default to
`trust: quarantined` (starter agent SCOPE R4); an operator approves
the bundle by content hash before this skill is selectable.
