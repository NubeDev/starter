{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "CustomersByCountryParams",
  "description": "Parameters for the `com.rubix.example.customers_by_country` warehouse-read template. Returns one row per country with the customer count, ordered desc.",
  "type": "object",
  "properties": {
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 200,
      "default": 25,
      "description": "Maximum number of country buckets to return."
    },
    "min_count": {
      "type": "integer",
      "minimum": 1,
      "default": 1,
      "description": "Drop countries with fewer than this many customers."
    }
  },
  "additionalProperties": false
}
