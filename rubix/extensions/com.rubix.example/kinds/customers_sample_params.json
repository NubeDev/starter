{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "CustomersSampleParams",
  "description": "Parameters for `com.rubix.example.customers_sample`. Returns up to `limit` raw customer rows from this extension's warehouse table, restricted to the caller's tenant.",
  "type": "object",
  "properties": {
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 500,
      "default": 100
    }
  },
  "additionalProperties": false
}
