# `com.nubeio.rubixos.bc_label_render`

Render a printable label payload for a provisioned device. Returns
a `rubix://add?…` QR url, a pipe-delimited Code128 string and the
device serial, ready for a label printer. Read-only and
tenant-scoped.
