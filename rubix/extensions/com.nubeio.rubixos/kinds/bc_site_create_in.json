{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "BcSiteCreateInput",
  "description": "Create a site. `row` carries `site_id` and `name`. `tenant_id` is bound by the host and cannot be supplied.",
  "type": "object",
  "properties": {
    "row": {
      "type": "object",
      "description": "Site row — `site_id`, `name`.",
      "additionalProperties": true
    }
  },
  "required": ["row"],
  "additionalProperties": false
}
