# com.rubix.geo — extension build + lifecycle.
#
# Delegates the canonical contract (build / pack / install / reload /
# uninstall / test / status / etc.) to ../extension.mk. See
# `docs/scope/extensions/installed-only-model.md` for the model.

EXT_ID    := com.rubix.geo
BIN_NAME  := rubix-geo-extension
# HAS_UI not set — this bundle has no ui-src/.

include ../extension.mk
