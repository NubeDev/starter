# 2026-05-24 — Goals 2, 4, 3 landed end-to-end

Closing session note for branch `codeless/rubix-goals-2-4-3`. Four
of six SCOPE goals now light up end-to-end through the real agent
loop; Goals 1 and 6 are stub-output verbs returning
`Diagnostic { code: rubix.goal.not_wired }` per
[THIN-SLICE.md §"Goals lit up beyond the thin slice"](../scope/THIN-SLICE.md).

## Per-goal verification

For each landed goal the following five evidence items were
captured during the closing gate:

1. **One `tools/call`** through the MCP surface that exercises a
   real write (verb listed below).
2. **One `rubix.undo.last`** that reverses the write (state
   restored from the bounded `undo_snapshots` row).
3. **Integration test count** — `tests/goal_<n>_*_test.rs` cases.
4. **`i18n_keys` boot-log line** — boot emits the count of loaded
   MessageKeys from `rubix-spi/catalogues/{en,es}.json` so the
   round-trip surface is verifiable from the log without grep.
5. **Snapshot sweep evidence** (Goal 4 only) — the bounded
   `undo_snapshots` table caps retained rows; oldest beyond the
   cap is swept on insert.

### Goal 2 — user-admin (Phase B)

- `tools/call`: `rubix.user.create` → row in PG `users`.
- Undo: `rubix.undo.last` → `disabled_at` set, `changelog` row
  with `kind = "undo"` references the original action id.
- Tests: `rubix-agent/tests/goal_2_user_admin_test.rs` — 2 cases
  (create-then-row-asserts, undo-then-disabled-asserts).
- Boot log: `i18n_keys=34` (EN catalogue) covering the six
  user-admin keys added in Phase B.2.
- Snapshot sweep: N/A (writes are PG row mutations; undo restores
  from `changelog.action_payload`).

### Goal 4 — clickhouse-ruler (Phase C)

- `tools/call`: `rubix.clickhouse.retention.set` → ALTER on the
  target ClickHouse table; `system.tables` shows new TTL.
- Undo: `rubix.undo.last` → prior TTL restored from the snapshot
  row written before the ALTER.
- Tests: `rubix-agent/tests/goal_4_clickhouse_ruler_test.rs` —
  3 cases (rule.write/mart.create/retention.set, each with the
  undo round-trip).
- Boot log: `i18n_keys=34` covers the six clickhouse-ruler keys
  added in Phase C.1.
- Snapshot sweep: `undo_snapshots` insert path drops the oldest
  row beyond `RUBIX_UNDO_SNAPSHOT_CAP` (default 100); the
  integration test asserts the cap by inserting one over the
  limit and checking `count(*) = cap`.

### Goal 3 — flow-programmer (Phase D)

- `tools/call`: `rubix.flow.duplicate` against
  `com.rubix.scheduled-system-check` → new revision row in
  `flows_definitions` under the target flow id; `tools/list`
  surfaces both flow ids.
- Undo: `rubix.undo.last` → new revision row marked
  `superseded_at = now()`; `tools/list` shrinks back to the
  pre-duplicate catalogue.
- Tests: `rubix-agent/tests/goal_3_flow_programmer_test.rs` —
  4 cases (deploy / lint / list / duplicate, each with the
  undo round-trip where applicable).
- Boot log: `i18n_keys=34` covers the six flow-programmer keys
  added in Phase D.2.
- Snapshot sweep: N/A (revisions are immutable rows; undo
  toggles `superseded_at` rather than deleting the row).

## Cross-instance NOTIFY (Goal 3)

`pg_notify('rubix_flows_definitions', ...)` fires on every insert
or `superseded_at` update; `rubix-agent/src/boot/flow_notify.rs`
listens and calls `FlowRegistry::reload(flow_id, body)`. Verified
in `flow_definitions_seed_test.rs` by writing through one pool
and asserting the second pool's registry reloads within the
default 5-second listen window.

## Goals 1 and 6 — stub-output verbs

Wired in this final closing-docs commit:

- `com.rubix.dashboard-assistant` →
  `rubix-tools/src/dashboard/assistant.rs` (Tool stub) +
  `flows/dashboard-assistant.yaml` `allowed_tools[0]` →
  `Diagnostic { code: rubix.goal.not_wired, params: {goal, design_doc} }`.
- `com.rubix.weekly-report` →
  `rubix-tools/src/analytics/weekly_report.rs` (Tool stub) +
  `flows/weekly-report.yaml` `allowed_tools[0]` →
  same shape, pointing at `docs/design/reports/README.md`.

EN + ES catalogue entries for `rubix.goal.not_wired` ship in the
same commit so the i18n round-trip stays green.

## PR shape

One PR off `codeless/rubix-goals-2-4-3` against `master`, reviewed
phase-by-phase (B → C → D → closing). PR creation is the next
operator-confirmation gate; see this branch's stage-16 handover.
