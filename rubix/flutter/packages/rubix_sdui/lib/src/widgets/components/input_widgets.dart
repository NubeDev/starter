/// Input widgets — `toggle`, `slider`, `select`, `*_field`,
/// `checkbox`, `segmented`, `radio_group`, `date_field`,
/// `date_range`, `ref_picker`, `rich_text`, `markdown_editor`,
/// `textarea`, `select_field`.
///
/// **Scaffold only.** Wave 1: toggle · slider · select ·
/// text_field · number_field · checkbox · segmented.
library;
