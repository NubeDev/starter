//! `rubix-data-postgres` — Phase-0 skeleton.
//!
//! Reserves the crate name and pins the Postgres testing seam
//! (`starter_store_postgres::testing::with_database`) via the
//! integration smoke under `tests/`. No production schemas yet
//! — those land from Phase 1 onward, alongside the kinds whose
//! state needs to persist.
#![forbid(unsafe_code)]
