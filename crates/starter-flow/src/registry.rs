//! `NodeKindRegistry` + `FlowRegistry`.
//!
//! SCOPE section: "What lands in `starter-flow`" crate block — the
//! two registries the engine resolves against at run time
//! (`NodeKindRegistry` keyed by reverse-DNS `KindId`, `FlowRegistry`
//! keyed by `FlowId` + `FlowRevisionId`).
//!
//! Phase-1 marker: empty — populated in Phase 3.
