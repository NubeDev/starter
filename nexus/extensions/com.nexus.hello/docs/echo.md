# `com.nexus.hello.echo_tool`

Echoes the input `message` back verbatim. A liveness probe for the supervised
process: a successful round-trip proves the host spawned the child, completed
the JSON-RPC handshake, and can dispatch tool calls into it.
