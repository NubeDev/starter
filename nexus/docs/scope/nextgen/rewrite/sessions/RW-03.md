# RW-03 — Cutover: runners on the native engine, ArkFlow deleted

Status: Done
Started: 2026-06-10 03:30 UTC
Finished: 2026-06-10 04:35 UTC

## First action — spec re-verification (mandatory)

Re-grepped every `file:line` the RW-03 spec "Current state" cites against the tree at
the start of this session. All claims hold; no spec drift, no `Verified:` bump needed:

- `runner/query.rs` builds an ArkFlow `StreamConfig` (`build_and_run`), `stream.run(token)`,
  drains the collector via `store::take`, enforces caps → JSON. (lines ~45-106).
- `runner/live.rs` `spawn` builds `{input, pipeline, output: sse}` and spawns
  `stream.run(token)` (lines ~33-55).
- `flow/manager.rs` `start` builds `{input, pipeline, output}`, spawns the ArkFlow stream,
  tracks `FlowStats`, `stop()` cancels the token (lines ~74-126).
- `runner/poll.rs` has no ArkFlow types — it is a pure interval poll loop over a
  caller-supplied producer; left unchanged by the cutover.
- `runner/cancel.rs` is a `CancellationToken` deadline timer; no ArkFlow types; unchanged.
- `backend/Cargo.toml:63-64` (git deps), `:116-121` (the `[patch.…]` vendor redirect),
  `backend/vendor/arkflow-plugin/` (the trimmed fork), `registry/mod.rs` ArkFlow `init()`.
- `stream_registry.rs` ~80-125 refcount/`attach`/`release`; no ArkFlow types — the live
  teardown is engine-agnostic and unchanged.

## TODOs.md blockers resolved before coding

- **sql JSON-UDF confirmation (RW-02 follow-up).** The vendored sql processor registers
  `datafusion_functions_json::register_all` (`vendor/arkflow-plugin/src/component/sql.rs:21`).
  Grepped the whole nexus tree (migrations, fixtures, seed, tests, stored-config samples)
  for every `json_*` UDF name — zero hits. No nexus flow, migration, or test uses a JSON
  UDF in `sql.query`. Dropping the dep is therefore safe; `datafusion-functions-json` is
  NOT added (would violate §8 with no consumer). Omission confirmed safe.

## Task breakdown

0. Core contract alignment (lane transferred from RW-01/02 per roadmap §4 + TODOs.md):
   - `core/node.rs`: `Processor::process(&mut self, …)` (was `&self`); drop the now-needless
     `Sync` bound the shared-`&self` model required.
   - `core/pipeline.rs`: thread `&mut` processors through the consume loop; enforce
     `max_batch_rows` zero-copy `RecordBatch::slice` at the source and processor-output
     boundary before a batch enters the channel; call the new `Source::commit()` after each
     successful sink write.
   - `core/config.rs`: parse `pipeline.max_batch_rows` (default 8192).
   - `Source::commit()` default-no-op added to `core/node.rs`.
   - Flip the two RW-02 processor impls (`processor/json_to_arrow.rs`, `processor/sql.rs`)
     to `&mut self` (mechanical — forced by the trait change).
1. Switch the three runners + dry-run onto `core::Pipeline`, keeping `QueryRunner::new`,
   `LiveRunner::new`/`spawn`, `FlowManager::new`/`start`/`stop` signatures and the JSON
   config shape FROZEN. Each runner holds an `Arc<core::Registry>` from `native_registry()`.
2. Replace `registry/mod.rs` ArkFlow `register_all` with the native registry handle; the
   descriptor palette (`registry/descriptor.rs`) drops the `arrow_to_json` entry — no native
   node builds it and no stored config uses it, so the palette now matches what the engine
   can actually construct.
3. Delete `arkflow-core`/`arkflow-plugin` deps, the `[patch.…]` block, `backend/vendor/`,
   and every ArkFlow-based source/sink/processor source file the native ports replaced.
4. Keep arrow/datafusion pinned to RW-02's resolved versions.

## Assumptions / deviations

- `arrow_to_json` removed from the node palette: it was an ArkFlow-only processor with no
  nexus consumer (grep-confirmed). The native sinks convert Arrow→JSON internally, so an
  in-pipeline `arrow_to_json` node is meaningless on the native engine.

## Acceptance verification

- `grep -ri arkflow nexus/backend --include='*.rs' --include='*.toml'` → zero hits. Also
  zero in `Cargo.lock` and the regenerated `openapi.json`.
- `backend/vendor/arkflow-plugin/` deleted (git rm, 35 files); both arkflow git deps and the
  `[patch."https://github.com/arkflow-rs/arkflow"]` block removed from `backend/Cargo.toml`.
- `cargo test --workspace` green: 207 passed, 0 failed, 1 ignored (pre-existing, unrelated).
- Stored-config parity: `tests/native/stored_config_parity_test.rs` runs a verbatim
  `{input, pipeline, output}` blob (parsed from a raw string, not a `json!` literal — the
  byte-for-byte shape a saved flow carries in the DB) through `Pipeline::run` and asserts the
  rows/columns come back unchanged. Proves flows created under the old engine still run.
- arrow/datafusion left at RW-02's pinned versions (datafusion 47); `Cargo.lock` only lost the
  arkflow entries, no resolution churn.
- DTO doc comments that named ArkFlow were reworded to "engine"; `openapi.json` regenerated via
  `cargo run --bin nexus-openapi` and the FE client via `pnpm codegen` (description-only diffs,
  no type-shape change).

## Commits

- RW-03: cutover runners/registry to native engine + delete ArkFlow (see git log)
