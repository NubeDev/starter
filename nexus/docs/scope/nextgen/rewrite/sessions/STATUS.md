# Nexus Rewrite — Build Status Board

> Single source of truth for the orchestration loop. The loop reads this file on every wake.
> Each session updates its own row when it starts, blocks, or finishes.
> All work lands on branch **`nexus-rewrite`** (sequential — one RW at a time, no worktrees).

**Legend:** ⬜ pending · 🔵 in-progress · ✅ done (build+tests green, committed) · ⛔ blocked (see [TODOs.md](./TODOs.md))

## Execution queue (dependency order — DO NOT reorder)

| Order | RW | Title | Status | Started | Finished | Commit | Notes |
|------:|----|-------|:------:|---------|----------|--------|-------|
| 1 | RW-01 | Engine core: native pipeline loop, node traits, registry | ✅ | 2026-06-10 02:20 UTC | 2026-06-10 02:58 UTC | 9757df7c | additive only; ArkFlow stays compiling; 5 core tests green |
| 2 | RW-02 | Port nodes onto core (DataFusion direct) | ✅ | 2026-06-10 02:30 UTC | 2026-06-10 03:25 UTC | 6b25bb7d | behavior parity, same registry names; 40 engine tests green |
| 3 | RW-03 | Cutover: runners on native engine; delete ArkFlow | ✅ | 2026-06-10 02:55 UTC | 2026-06-10 04:35 UTC | 8d679c5b | grep-zero arkflow; vendor/ gone; 207 tests green |
| 4 | RW-04 | Any-DB store: datasource-id sinks, batched writes | ✅ | 2026-06-10 03:25 UTC | 2026-06-10 03:47 UTC | 70a48deb | postgres COPY + file(parquet) writers; audited resolve |
| 5 | RW-05 | Federation: DataFusion across datasources + file kinds | ✅ | 2026-06-10 03:55 UTC | 2026-06-10 11:30 UTC | a8dc7274 | hand-written sqlx TableProvider + native parquet/csv; push-down path byte-identical; file *persistence* deferred to RW-04 (TODOs.md) |
| 6 | RW-06 | nexus-insights: DataFusion engine + Rhai sandbox + query stage | ✅ | 2026-06-10 04:25 UTC | 2026-06-10 14:40 UTC | d7d573bb | DataFusion (not Polars) — every primitive lowers to one SQL stmt, none grows rows; Rhai sandbox (op/depth/string/deadline caps, no fs/net/eval/import); insight applied post-cache before serialize; `2101_insights.sql` RLS; openapi +273 add-only, UI green |
| 7 | RW-07 | Extension data-plane: sources/sinks/insights contributions | ✅ | 2026-06-10 05:00 UTC | 2026-06-10 15:25 UTC | 167c1059 | PARTIAL — insights slice (items 1+5) done: `contributes.insights[]` boot lint+materialise+cleanup into global `nexus_extension_insights` (2201, no RLS, mirrors query-kinds), `InsightRef.insight_name` query path, hello.zscore demo; openapi +8 add-only. Items 2–4 (sources/sinks/`ingest.write`/engine nodes/authz) DEFERRED → TODOs.md (2026-06-10 RW-07) |
| 8 | RW-08 | Backpressure hardening + soak + flow metrics | ✅ | 2026-06-10 05:20 UTC | 2026-06-10 06:35 UTC | baf596e1 | metered-wrapper FlowMetrics (6 counters) + on_error halt\|drop\|dlq + source retry_backoff; SSE lag-shed test; #[ignore] soak (`make soak`); BACKPRESSURE.md; openapi +46 add-only |
| 9 | RW-09 | Transport breadth: http_ingest + Zenoh source | ✅ | 2026-06-10 05:50 UTC | 2026-06-10 16:40 UTC | 799040c3 | http_ingest push path (`POST /api/v1/ingest/{flow_id}`, bounded channel via `IngestChannels` on FlowManager, 429+Retry-After, cross-tenant→404) + feature-gated zenoh source (at-most-once, loopback peer test); zenoh deps ZERO by default (cargo tree proof); openapi +61 add-only, UI green; `ingest.write` reuse + zenoh store-probe → TODOs.md |
| 10 | RW-04b | File-datasource persistence (RW-04 fix pass) | ✅ | 2026-06-10 06:20 UTC | 2026-06-10 07:30 UTC | 5ac2cbec | migration `2001_datasource_file_config.sql` (nullable secret/connection cols + `config jsonb`); record/insert/fetch carry `config`+optional secret; `federation::resolve` parquet/csv arms build from `config.path`; stored-Parquet⋈PG e2e (#[ignore] docker); `DatasourceKind`+`Mqtt`/`Zenoh`, `test_connection` mqtt+zenoh arms via feature-gated store `zenoh` probe (0 deps default, mirrors mqtt); openapi add-only/relaxation, UI codegen+typecheck green; cargo test --workspace green |
| 11 | RW-07b | Extension ingest data-plane: sources/sinks + ingest.write (RW-07 items 2-4) | ⬜ | | | | supervisor-queued from RW-07's deferral TODO; spec = RW-07_EXTENSION_DATA_PLANE.md items 2-4 + the TODOs.md entry "Extension data-plane sources/sinks deferred" (incl. its outstanding acceptance bullets: tenant-stamp e2e, channel-full retry_after test, hello purge assertion); migration 2202+; completes the human's "incoming is generic" requirement |

## Loop log (append one line per wake)

<!-- Format: `YYYY-MM-DD HH:MM — <action taken>` -->
2026-06-10 02:20 — RW-01 picked (first pending, no RW in progress); marked 🔵, spawning subagent.
2026-06-10 02:58 — RW-01 done: nexus-engine/src/core/ landed (node traits, registry, config, pipeline, outcome, error); 5 core tests + full engine suite green; ArkFlow still compiles. Marked ✅.
2026-06-10 02:30 — RW-02 picked (first pending, no RW in progress); marked 🔵, spawning subagent.
2026-06-10 03:25 — RW-02 done: native ports of every source/processor/sink onto core traits + native built-ins (memory/generate/http_poll/simulator/json_to_arrow/sql/collector/sse/postgres/drop/stdout) under same registry names; ArkFlow still compiles in parallel; 40 engine tests + workspace test green, clippy clean. Committed 6b25bb7d. Marked ✅. Three RW-01 core deltas (&mut self / commit() / max_batch_rows slice) + sql JSON-UDF confirmation logged to TODOs.md for RW-03.
2026-06-10 02:55 — RW-03 picked (first pending, no RW in progress); marked 🔵, spawning subagent.
2026-06-10 04:35 — RW-03 done: runners (query/live/flow-manager) + registry cut over to core::Pipeline; core §6 deltas landed (Processor::process(&mut self), Source::commit() no-op hook, max_batch_rows zero-copy slice); ArkFlow git deps + [patch] block + vendor/arkflow-plugin/ (35 files) + every arkflow source file deleted; grep -ri arkflow over backend .rs/.toml/.lock + openapi.json = zero; stored-config parity test proves old-engine flows run unchanged; 207 workspace tests green, 0 failed. openapi.json + FE client regenerated (description-only). Marked ✅.
2026-06-10 03:25 — RW-04 picked (first pending, no RW in progress); marked 🔵, spawning subagent.
2026-06-10 03:47 — RW-04 done: `datasource` sink (sink/datasource/: writer trait, postgres COPY writer via sqlx PgCopyIn text format, parquet rotating part-files, batch accumulator flush-on-rows-or-timer, strict identifier guard) registered in native_registry; secret resolution stays in store (resolve_sink_config, audited open_secret) + api (resolve_flow_output) with flows/start.rs as the thin transport seam — engine keeps zero nexus-store dep. Legacy postgres sink configs still build (parity test). e2e docker flow lands rows via COPY; parquet read back via DataFusion. Full engine suite + workspace green, clippy clean. No migration/DTO/codegen needed. Palette-descriptor follow-up logged to TODOs.md (RW-03 registry/ lane). Marked ✅.
2026-06-10 03:55 — RW-05 picked (first pending, no RW in progress); marked 🔵, spawning subagent.
2026-06-10 11:30 — RW-05 done: nexus-engine/src/federation/ (FederatedQuery runner — hand-written sqlx Postgres TableProvider + DataFusion-native parquet/csv, each registered as ds_<alias> so DataFusion's own catalog plans the join; GreedyMemoryPool input bound + Caps output bound + read-only SQLOptions). Dispatch seam in routes/datasources/query.rs: is_federated → federation::respond, else push-down UNCHANGED (byte-identical, unit-proven). Additive DTO QueryRequest.sources + FederatedSourceRef (openapi.json + UI codegen regenerated, +32 lines additive, UI typecheck green). parquet/csv kinds added to the WS-08b pack (GET /datasources/kinds lists them). Tenancy: resolve authorises every source against the caller's tenant (RLS NotFound + view grant), cross-tenant ref denied with no leak. Engine federation test (Parquet⋈CSV in one SQL, capped, truncation) + dispatch + pack unit tests GREEN; docker e2e (2-datasource join + cross-tenant denial) compiles green. Dep decision: rejected datafusion-table-providers (2nd PG client stack + df 53 bump, both §8-forbidden) — no new heavy dep, no datafusion bump. File-datasource *persistence* (store schema is Postgres-shaped) deferred to RW-04 fix pass — logged to TODOs.md, file resolve arms return explicit Invalid (loud) until then. Marked ✅.
2026-06-10 04:25 — RW-06 picked (first pending, no RW in progress); marked 🔵, spawning subagent.
2026-06-10 05:00 — RW-07 picked (first pending, no RW in progress); marked 🔵, spawning subagent.
2026-06-10 15:25 — RW-07 done (PARTIAL — insights slice, items 1+5): starter-ext-spi additive `Contributes.insights[]` + `ContributeInsight{name,script_file,params_schema?}` (3 tests). Global `nexus_extension_insights` (2201, no RLS, mirrors `nexus_extension_query_kinds`) + `nexus-store/src/extension_insight/**` (upsert idempotent/cross-owner-Conflict, get_by_name, list/delete_by_extension). nexus-api `contribute_insights.rs` (read script_file → namespace ownership R4 → RW-06 compile_check → NewExtensionInsight) + `cleanup_insights.rs` (InsightCleanupProvider) wired into boot; `ContributionPostInstall` materialises query-kinds AND insights in one PostInstallHook. Additive DTO `InsightRef.insight_name` (precedence id>name>script); apply.rs resolves it via get_by_name (global, no tenant; runs on caller's own rows; missing→404). openapi +8 add-only, UI codegen green. com.nexus.hello ships `com.nexus.hello.zscore`. New docker-gated e2e `routes_query_extension_insight_e2e` (name resolves+shrinks+stats recomputed; unknown name→404). cargo test --workspace green. Items 2–4 (sources/sinks/`ingest.write`+`ingest.read_batch`/engine extension nodes/`ingest.*` authz) DEFERRED with handoff → TODOs.md. Marked ✅.
2026-06-10 05:20 — RW-08 picked (first pending, no RW in progress); marked 🔵, spawning subagent.
2026-06-10 06:35 — RW-08 done: per-flow ingest metrics (flow/metrics.rs — batches_in/rows_written/channel_depth/flush_count/write_errors/last_write_ms) + failure policy (flow/policy.rs — on_error halt|drop|dlq, source_on_error retry_backoff|halt, capped exp backoff) implemented as DECORATOR metered wrappers (flow/metered/{source,sink,registry}.rs) installed via per-flow registry composed from public Registry/native_registry — ZERO core/pipeline edits, fully in flow/** lane. dlq routes to RW-04 file(Parquet) writer. FlowManager retains per-run FlowMetrics; FlowStats.metrics snapshot surfaced via additive DTO FlowMetrics fields (openapi +46 add-only, UI codegen+typecheck green). SSE load-shed verified+tested (Lagged per-receiver, seq gap visible, fresh subscriber unaffected). #[ignore] soak (tests/soak/{backpressure_soak,rss}.rs — steady RSS-bounded+lossless, fat-batch 2M sliced, db-fault halt reconciles) via `make soak` (needs docker). BACKPRESSURE.md one-pager. cargo test --workspace green. Out-of-lane pre-existing --features testing drift (identity/wiring, authz/grant_gate) logged to TODOs.md. Marked ✅.
2026-06-10 05:20 — loop wake: spawned RW-08 subagent; it ran to completion, self-gated ✅ (pushed b4ed279a). One workstream this wake. Next pending: RW-09.
2026-06-10 05:50 — RW-09 picked (first pending, no RW in progress; RW-08 already gated ✅); marked 🔵, spawning subagent.
2026-06-10 05:50 — loop wake: spawned RW-09 subagent; it ran to completion, self-gated ✅ (pushed 799040c3). One workstream this wake. Next pending: RW-04b.
2026-06-10 06:20 — RW-04b picked (first pending in queue order; all RW-01..RW-09 ✅, none 🔵, no STOP); marked 🔵, spawning subagent.
2026-06-10 07:30 — loop wake: spawned RW-04b subagent; it ran to completion, self-gated ✅ (pushed 5ac2cbec, 83b0dcf1; cargo test --workspace green; both source TODOs marked ✅ RESOLVED). One workstream this wake. Next pending: RW-07b.
2026-06-10 07:30 — RW-04b done: file-datasource persistence (migration 2001 — nullable secret/connection cols + `config jsonb`; record/insert/fetch carry `config` + optional secret; `federation::resolve` parquet/csv arms resolve `FederatedSource::{Parquet,Csv}` from `config.path`, loud Invalid on missing path; stored-Parquet⋈PG #[ignore]-docker e2e via secret-less store insert) + stream-connector probes (`DatasourceKind`+`Mqtt`/`Zenoh`, `TestConnectionRequest` SQL fields → Option + `config`, `test_connection` mqtt+zenoh arms; new feature-gated store `datasource/zenoh/probe.rs` mirroring mqtt — 0 zenoh deps by default, 1 under `--features zenoh`). openapi add-only/relaxation, `pnpm codegen`+typecheck green; UI DatasourceFormDialog drift fix. cargo test --workspace green; zenoh-gated store lib 6/6. No new TODOs. Marked ✅.
2026-06-10 16:40 — RW-09 done: `source/http_ingest.rs` (IngestChannels on FlowManager, per-flow `bind_http_ingest`, non-blocking try_push) + thin `POST /api/v1/ingest/{flow_id}` route (200 accepted / 429+Retry-After on full / 404 unknown+not-running+cross-tenant — no existence leak); feature-gated `source/zenoh.rs` (at-most-once no-op commit, JSON-verbatim/{binary} decode, in-process peer loopback test, transport_tcp + multi-thread rt); zenoh datasource-kind manifest entry (Stream, no secrets); IngestAccepted DTO; openapi +61 add-only, UI codegen+typecheck green. Default build carries ZERO zenoh deps (cargo tree 0 vs 81 gated). cargo test --workspace green (121 lib + suites; http_ingest 3/3) + zenoh-gated 1/1. BACKPRESSURE.md push-path + at-most-once sections. `ingest.write` reuse-seam (RW-07b) + zenoh store-probe (RW-04) → TODOs.md. Marked ✅.
2026-06-10 14:40 — RW-06 done: crates/nexus-insights (DataFusion-first vectorized engine — MemTable + SQL lowering per primitive: project/filter/rolling_{mean,min,max,sum}/lag/diff/pct_change/zscore/resample(date_bin)/head/tail/sort/fill_null/anomalies/describe, none grows rows so Rhai size caps stay meaningful; Rhai sandbox via Engine::new_raw + op/call-level/string/array/map/depth caps + wall-clock on_progress deadline + DummyModuleResolver, no fs/net/eval/import; sync Rhai in spawn_blocking bridging async DataFusion via block_on; structured Compile/Runtime/LimitExceeded/Engine errors). nexus-spi: additive QueryRequest.insight + insight CRUD DTOs (openapi.json regenerated +273 add-only, UI codegen + typecheck green). Migration 2101_insights.sql (RLS FORCE like folders) + nexus-store/src/insight/** + KIND_INSIGHT authz. Routes: /api/v1/insights CRUD (save-time compile_check; get/update/delete authz-gated) + query-path seam in both POST /query (dev, inline-only) and POST /datasources/:id/query (tenant), applied post-cache before serialize, stats recomputed. Insight intentionally NOT in cache key (applied post-cache per request). Tests: 26 nexus-insights green + 2 new API e2e binaries (insights CRUD/RLS, query+insight inline filter/passthrough/runaway→400) compile clean. Dep decision: DataFusion over Polars (§8 — no 2nd Arrow stack); only new heavy dep is §8-approved rhai. Pre-existing stale-drift in 2 out-of-lane test files logged to TODOs.md. Marked ✅.
