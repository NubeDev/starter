# RW-06 — nexus-insights: DataFusion engine + Rhai sandbox + query stage

Status: Done
Started: 2026-06-10 12:05 UTC
Finished: 2026-06-10 14:40 UTC

## First action — spec re-verification

Re-grepped every "Current state" claim the RW-06 spec relies on against the tree at
HEAD of `nexus-rewrite`:

- `crates/nexus-engine` SessionContext-in-tree (RW-02) — confirmed; DataFusion 47 pinned
  in `nexus/backend/Cargo.toml`. Engine exposes `arrow_json::{batch_to_rows, columns_of}`
  and `federation::rows_to_batch::json_rows_to_batch` (rows→RecordBatch), both reusable
  for the insight stage's JSON↔Arrow boundary.
- Query dispatch seam `crate::federation::{is_federated, respond}` in
  `nexus-api/src/routes/datasources/query.rs` — confirmed (RW-05 landed).
- Folders CRUD vertical (store `nexus-store/src/folder/**`, route
  `nexus-api/src/routes/folders/**`, migration `0602_folders.sql` with RLS) — confirmed;
  used as the template for the `insights` vertical.
- Migration numbering: latest is `1801`. Roadmap §5 reserves `21xx` for RW-06. The STATUS
  board row note ("migration 18xx") is stale; the spec body (scope item 3) correctly says
  `21xx`. Used `2101_insights.sql`.
- `QueryRequest` (`nexus-spi/src/dto/query/run.rs`) and `QueryResponse` shapes confirmed;
  insight field added additively, mirroring the RW-05 `sources` precedent.

No spec drift required a doc edit beyond noting the STATUS-row staleness (not my lane to
rewrite another row's note).

## Engine decision (roadmap §8 — DataFusion-first)

Implemented the vectorized backend on **DataFusion** (already in-tree, Arrow-native, no
second Arrow stack). Polars was NOT adopted: every first-set primitive maps to a DataFusion
window/aggregate/projection expression over a `SessionContext`, and `resample` is `date_bin`
+ group-by (the Timescale-shaped path). Adopting Polars would add polars-arrow/arrow2 — the
exact dep-bloat §8 forbids — for no ergonomic win. No new heavy dep beyond `rhai` (§8-approved).

## Task breakdown

1. `crates/nexus-insights` crate: `engine.rs` (DataFrame-handle over DataFusion),
   `sandbox.rs` (Rhai engine factory with operation/depth/string/array caps + wall-clock
   deadline via `on_progress`, DummyModuleResolver, no fs/net/eval), `api.rs` (curated
   vectorized surface registered into Rhai), `run.rs` (`run_insight`), `error.rs`
   (compile/runtime/limit structured errors).
2. nexus-spi DTOs: insight CRUD + `QueryRequest.insight` field; openapi registration.
3. Migration `2101_insights.sql` (RLS) + `nexus-store/src/insight/**` vertical.
4. Query route: apply the optional insight to result rows after `run_cached`, before serialize.
5. CRUD routes + `KIND_INSIGHT` authz constant.
6. Tests: per-primitive golden frames, sandbox kill-switches, e2e query+insight, RLS.

## Outcome

- `crates/nexus-insights` shipped: `engine/**` (DataFusion MemTable + SQL lowering for every
  primitive — project/filter/window/resample/shape/anomaly, each one SQL statement, none grows
  rows), `sandbox.rs` (op/depth/string/array/map caps + wall-clock deadline + DummyModuleResolver
  + `Engine::new_raw`, no fs/net/eval/import), `api.rs` (curated Frame surface), `run.rs`
  (`run_insight`/`run_insight_rows`/`compile_check`, sync Rhai in `spawn_blocking` bridging async
  DataFusion via `block_on`), `error.rs` (Compile/Runtime/LimitExceeded/Engine).
- nexus-spi: additive `QueryRequest.insight: Option<InsightRef>`; insight CRUD DTOs
  (`InsightRef`/`CreateInsightRequest`/`InsightSummary`/`UpdateInsightRequest`) registered in
  openapi; `openapi.json` regenerated (add-only, +273 lines) and UI codegen + typecheck green.
- Migration `2101_insights.sql` (tenant-scoped `nexus_insights`, RLS FORCE like folders) +
  `nexus-store/src/insight/**` vertical + `KIND_INSIGHT` authz constant.
- Routes: `/api/v1/insights` CRUD (save-time `compile_check` so a stored insight is always
  runnable; get/update/delete authz-gated on `KIND_INSIGHT`). Query-path seam: both `POST /query`
  (dev, inline-only, tenant=None) and `POST /datasources/:id/query` (tenant path) apply the
  insight after `run_cached` and before history/serialize, recomputing stats from the transformed
  rows. The insight runs *after* the cache, so the cache stores raw query results unchanged.
- Tests: 26 nexus-insights tests green (15 primitive golden frames, 7 sandbox kill-switches incl.
  infinite-loop/huge-string/deadline/import-disabled, 4 run_insight incl. params + realistic
  anomaly pipeline). New API e2e: `routes_insights_e2e` (CRUD round-trip, uncompilable-script
  rejected, cross-tenant RLS isolation) and `routes_query_insight_e2e` (inline filter shrinks
  result + recomputes stats, passthrough intact, runaway script → clean 400). Both compile clean;
  the DB-backed ones are `#[ignore = "requires docker"]` per house convention.

## Assumptions / deviations / follow-ups

- Cache key (`cache/key.rs`) intentionally does NOT fold in the insight: the insight is applied
  post-cache on every request, so two queries differing only by insight share the raw cached
  rows and each transforms its own copy. Documented so a later cache change does not "fix" this.
- Two pre-existing nexus-api test binaries (`routes/authz/grant_gate_test.rs`,
  `routes/identity/wiring_test.rs`) fail to compile due to stale `NewDashboard`/`serve::assemble`
  drift from earlier RWs — outside RW-06's lane, not caused by the additive `insight` field.
  Logged in TODOs.md (2026-06-10 RW-06) for the owning RW to fix; RW-06's own test binaries are
  unaffected.
