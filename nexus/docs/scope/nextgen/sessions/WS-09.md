# WS-09 — Production Hardening

> Spec: [WS-09_PRODUCTION_HARDENING.md](../WS-09_PRODUCTION_HARDENING.md)
> Branch: `nexus-gaps` (sequential, no worktrees)

**Status:** In-progress
**Started:** 2026-06-10T00:00Z
**Finished:** —

## Re-verification (ROADMAP §0)

Re-grepped the spec's "Current state" evidence at branch tip:

- ✅ `/health`/`/metrics`/tracing: `starter-server` ships them; nexus' `serve.rs` builds a bare
  `ServerBuilder` and does **not** call `with_metrics`, so nexus does not currently export
  `/metrics`. Spec says "✅ exist (main.rs, serve.rs)" — partially stale: the *capability* exists in
  starter-server, but nexus has not wired it. Recorded here; not blocking the cache work (cache
  stats are exposed as atomics readable by tests).
- ✅ Query guards + per-`(tenant,datasource)` pool cache: `datasource_pools.rs` confirmed.
- ✅ No query-result cache anywhere — confirmed (`grep moka/redis/memo` empty).
- ✅ No rate limiting / quotas — confirmed.
- ✅ Query history exists (WS-03 shipped `query_history` + `/datasources/:id/query` records it).
- 🐞 Login-hang: confirmed — `starter-auth-users/src/password/verify.rs` + `hash.rs` are **sync**
  functions; called synchronously from `routes/login.rs:110` and `token/verify.rs:33` inside async
  fns. The argon2 work runs on the async runtime. Location matches the spec's verified note.

The C3 cache-key inputs all exist on `QueryRequest` (sql/kind/params/time_range/interval/variables)
and `QueryIdentity` (tenant_id/user_id) — confirmed in `nexus-spi/dto/query/run.rs` and
`nexus-store/query/request.rs`.

## Scope decision for this unattended session

WS-09 is a multi-wave umbrella (P0 login fix · P1 cache/quota/rate-limit · P2 history/OTel ·
P3 HA/multi-node). P3 (SSE shared bus, multi-node leader) is explicitly "its own sub-project";
OTel touches `main.rs`/`serve.rs` plumbing broadly. The headline production value is the **P1
result cache** (the DB-melt problem) plus **per-tenant concurrency** and **rate-limiting**. I land
those, properly tested, inside the WS-09-owned lane (`nexus-api/src/{cache,quota,ratelimit}/**`,
`middleware/**`).

- **P0 login-hang → TODOs.md (not implemented here).** The fix lives in `starter-auth-users`, a
  **shared crate** used by every app on the platform; the spec itself states it "needs an upstream
  PR + sign-off, not a nexus-local patch." Per this run's HARD RULES (stay in lane; a shared
  contract/crate change needing sign-off is a TODO, not a guess), I log it rather than edit a
  crate outside my ownership row.
- **P2 OTel / P3 HA → TODOs.md follow-ups** (own sub-project; broad `main.rs`/`serve.rs` rewiring).

## Task breakdown

1. `cache/` module — C3-keyed in-process TTL cache with single-flight coalescing + hit/miss
   atomics; cache key carries `units_locale_tz` (constant placeholder per D4) from day one.
2. Wire the cache at the `kinds::run` seam so both `/query` and `/datasources/:id/query` benefit;
   bypass when the caller opts out of caching.
3. `quota/` module — per-tenant concurrency cap (semaphore) with a clear "busy" error on exceed.
4. `ratelimit/` module — per-tenant token-bucket request limiter as middleware.
5. Mirrored tests for each; keep `cargo test` + UI gates green.

## Commits

(to be filled in)

## Assumptions / deviations / follow-ups

- `units_locale_tz` is a constant placeholder (`"default"`) until WS-11 threads resolved per-user
  prefs into the query path — exactly the D4 "bake the field in now" requirement. A test asserts a
  changed placeholder yields a distinct key so enabling WS-11 later cannot serve a poisoned entry.
- Follow-ups logged to TODOs.md: P0 login-hang (shared-crate, sign-off), P2 OTel, P3 HA/SSE-bus,
  `with_metrics` wiring so cache counters reach `/metrics`.
</content>
</invoke>
