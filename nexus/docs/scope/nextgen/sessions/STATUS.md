# Nexus Next-Gen — Build Status Board

> Single source of truth for the orchestration loop. The loop reads this file on every wake.
> Each session updates its own row when it starts, blocks, or finishes.
> All work lands on branch **`nexus-gaps`** (sequential — one WS at a time, no worktrees).

**Legend:** ⬜ pending · 🔵 in-progress · ✅ done (build+tests green, committed) · ⛔ blocked (see [TODOs.md](./TODOs.md))

## Execution queue (dependency order — DO NOT reorder)

This is the order the loop starts sessions. It follows ROADMAP §2/§3 waves so a later
session always finds its dependencies already committed in the working tree.

| Order | WS | Title | Status | Started | Finished | Commit | Notes |
|------:|----|-------|:------:|---------|----------|--------|-------|
| 1 | WS-03 | Query Authoring + Macro Engine (C2 binder) | ✅ | 2026-06-09 11:58 | 2026-06-09 12:21 | ac76c1de | binder+history; B/C pre-shipped; unblocks 01/02/10 |
| 2 | WS-04 | Panel Editor & Viz Config | ✅ | 2026-06-09 12:40 | 2026-06-09 20:05 | d27f293f | full editor + transforms + field config |
| 3 | WS-07 | Alerting depth | ✅ | 2026-06-09 13:05 | 2026-06-09 13:30 | f52d6b38 | multi-condition + no-data/error policy + slack/email + templating + retry |
| 4 | WS-10 | Kinds — declarative extensibility | ✅ | 2026-06-09 13:30 | 2026-06-09 13:55 | dd4687ae | registry+loader+lints+pack(4)+kind-mode dispatch+catalogue route+UI picker; reuses WS-03 binder |
| 5 | WS-08 | Connector breadth (MQTT/Modbus/…) | ⛔ | 2026-06-09 14:00 | 2026-06-09 21:20 | 00e40609 | pre-save `/datasources/test` landed+GREEN; connectors blocked on WS-10 datasource-kind format + gated-deps decision (TODOs.md) |
| 6 | WS-01 | Time Range & Auto-Refresh | ✅ | 2026-06-09 14:20 | 2026-06-09 21:35 | 8e5a2cdf | UI-only: backend passthrough pre-shipped by WS-03; picker+refresh+URL+query wiring; zoom-by-drag deferred (TODOs.md) |
| 7 | WS-02 | Variables & Templating | ✅ | 2026-06-09 14:40 | 2026-06-09 22:10 | db0cee76 | defns table(0701,RLS)+CRUD routes+resolution(cascade+cycle-detect)+bar/editor+URL+folded into useWidgetQuery; cargo build/test + pnpm typecheck/test(197)/build GREEN; repeat-render→WS-05 |
| 8 | WS-11 | Units & datetime prefs | ✅ | 2026-06-09 15:10 | 2026-06-09 22:40 | 9094aea9 | backend vertical: PG prefs store + route-pinned `/me/preferences` + Accept-Units mounted; series quantity-tagging/UI screen/alerting coupling → follow-ups (TODOs.md) |
| 9 | WS-12 | Audit log + undo/redo | ✅ | 2026-06-09 15:40 | 2026-06-09 23:30 | 224027ec | tenant-scoped+RLS changelog(1601-1603) substrate; recorder/log/registry; dashboard+datasource Reversibles+coverage guard; audit+undo+redo+GDPR-forget routes; retention prune sweep; undo/redo Cmd+Z UI + Audit screen + diff + History tab. cargo lib(70)+audit-e2e(7,docker) + pnpm typecheck/test(202)/build GREEN. delete-inverse + grant_gate drift → TODOs.md |
| 10 | WS-05 | Folders/Rows/Repeat/JSON/Versioning | ✅ | 2026-06-09 16:31 | 2026-06-09 | (pending) | folders vertical (0602)+folder_id/starred (0603)+id-stable inserts+duplicate+JSON export/import+C6 dashboard&folder Reversibles (coverage guard green)+UI data layer/hooks. cargo --workspace --lib(73)+pnpm typecheck/test(202)/build GREEN. rows/repeat/public-share/version-UI + folder-tree component UI → TODOs.md. alert_crud pre-existing break flagged |
| 11 | WS-06 | Flows Visual Builder | ⬜ | | | | needs WS-08 palette |
| 12 | WS-09 | Cache/Audit/Rate-limit/HA/OTel | ⬜ | | | | cache key needs C1/C3 |

## Loop log (append one line per wake)

<!-- Format: `YYYY-MM-DD HH:MM — <action taken>` -->
2026-06-09 11:58 — Loop start. Queue empty → spawned WS-03 (first in dependency order) on nexus-gaps.
2026-06-09 12:21 — WS-03 done (commits 855db5ba, ac76c1de). C2 binder + query history landed; Scope B/C were already shipped at base (drift recorded). cargo test + pnpm build/test green; pnpm typecheck blocked only by a pre-existing out-of-lane starter-ui-authz error (TODOs.md). Next: WS-04.
2026-06-09 12:40 — WS-03 ✅, no WS in-progress. Picked first pending in queue order → spawning WS-04 (Panel Editor & Viz Config) on nexus-gaps.
2026-06-09 12:41 — Wake skipped: WS-04 already 🔵 (claimed by a concurrent in-flight wake; heartbeat wake-start ~1min old). No double-spawn. Yielding.
2026-06-09 13:05 — WS-03 ✅ & WS-04 ✅, no WS in-progress, no STOP. Picked first pending in queue order → spawning WS-07 (Alerting depth) on nexus-gaps.
2026-06-09 13:31 — Wake complete: WS-07 subagent returned Done within this wake; gate green and row marked ✅ (f52d6b38). One WS spawned this firing. Next pending → WS-10 (Kinds) on the following wake.
2026-06-09 13:30 — WS-07 ✅. Alerting depth: multi-condition rules (AND/OR, per-condition reducer), per-rule no-data/exec-error policy resolved before the untouched state machine, Slack + Email (SMTP/lettre+rustls) channels alongside webhook, safe single-pass message templating, retry-with-backoff delivery recording per-channel outcome. Fixed a real UI↔backend operator-name mismatch (dialog sent `>`/`>=`, comparator expects gt/gte). DTO-first: openapi.json + TS client regenerated (merged with the concurrent agents schema already at HEAD). cargo build + 36 alerting unit tests green; pnpm typecheck/test(134)/build green. Committed only WS-07 hunks; left the agents session's uncommitted `lib.rs` `pub mod agents;` hunk untouched. C6 (audit/undo) deferred — WS-12 substrate not yet run.
2026-06-09 13:30 — WS-07 ✅, WS-03/04 ✅, no WS in-progress, no STOP. Picked first pending in queue order → spawning WS-10 (Kinds — declarative extensibility) on nexus-gaps.
2026-06-09 13:55 — Wake complete: WS-10 subagent returned Done within this wake; row already ✅ (dd4687ae + 7a442053). Declarative query-kinds: registry+loader+two boot lints+core pack(4 kinds)+kind-mode dispatch+`GET /query/kinds` catalogue+UI KindPicker; reuses WS-03 C2 binder, tenant isolation via host-bound `$caller_tenant_id`. cargo build + `cargo test --workspace --lib` (19 new) green; pnpm typecheck/test(134)/build green. Committed only WS-10 hunks. Flagged pre-existing cross-lane breakage in WS-10.md: docker-gated `tests/routes/authz/grant_gate_test.rs` fails to compile (NewDashboard gained accent/icon from dashboard WS) — left for that owner. One WS spawned this firing. Next pending → WS-08 (Connector breadth) on the following wake.
2026-06-09 14:00 — WS-03/04/07/10 ✅, no WS in-progress, no STOP. Picked first pending in queue order → spawning WS-08 (Connector breadth — MQTT/Modbus/…) on nexus-gaps.
2026-06-09 20:05 — WS-04 ✅. Full-screen Panel Editor (Query/Visualization/Field/Overrides/Legend & Axes/Transforms) with live preview, opened from the side panel. UI-only: panel config rides the opaque layout JSON (no migration, no backend DTO). Field config (unit/decimals/min-max/multi-step thresholds/value mappings) + per-series overrides (colour/unit/displayName/hidden) honoured by gauge/stat/table/line/bar; client transform pipeline (rename/calculated/filter/group-by/reduce/organize) runs after fetch so config edits re-render from cache. pnpm typecheck + test (134 pass) + build green. Committed only WS-04 UI hunks (backend agent/* changes belong to a concurrent session — left untouched).
2026-06-09 14:00 — Wake complete: WS-08 subagent returned within this wake, self-gated to ⛔ blocked (TODOs.md logged: connectors need WS-10 datasource-kind format + gated-deps decision). Pre-save `/datasources/test` landed GREEN (00e40609). Block honored — no ✅. One WS spawned this firing. Next pending → WS-01 (Time Range & Auto-Refresh) on the following wake.
2026-06-09 14:20 — WS-08 ⛔ (block honored, unresolved in TODOs.md), WS-03/04/07/10 ✅, no WS 🔵, no STOP. Picked first pending in queue order → spawning WS-01 (Time Range & Auto-Refresh) on nexus-gaps.
2026-06-09 21:20 — WS-08 ⛔ partial. Landed the pre-save `POST /datasources/test` acceptance criterion in full: `TestConnectionRequest` DTO → openapi+codegen, a `nexus-store` raw-config `probe()` (single short-lived conn, `SELECT 1`, 10s-bounded, secret dropped, nothing persisted/audited), a thin `test_connection` route (principal-gated, dispatches on kind, shared sanitizer extracted to `probe_outcome.rs`), and a "Test connection" button + `ProbeResult` banner in the create form. Mirrored tests: non-docker closed-port probe, docker-gated success + route e2e, and the UI banner. cargo build/lib-tests/clippy green; pnpm typecheck/test(139)/build green. Connectors themselves BLOCKED — vendored ArkFlow is connector-trimmed (MQTT/Modbus need new gated deps `rumqttc`/`tokio-modbus` + nexus-authored Input impls), the kind-specific config shape is the WS-10 datasource-kind format (Wave 2, not yet built), and query connectors need the PgPool query core reshaped (WS-03 lane). Recommend running WS-10 datasource-kinds first; see TODOs.md. C6 audit/undo deferred (WS-12 substrate not run). Committed only WS-08 hunks.
2026-06-09 21:35 — WS-01 ✅. Global time range + auto-refresh, UI-only. Re-verify found the WS doc's "`POST /query` accepts raw `sql` only" claim stale — WS-03 pre-shipped the C7 `QueryRequest` (`time_range`/`interval_secs`/`variables`) + binder + codegen'd TS types, so the backend passthrough already exists; fixed the WS doc + bumped its `Verified:` line. Landed: a pure relative-token resolver (`store/time/resolve.ts` — `now`/`now-6h`/`now/d`, calendar-aware floors) + `$__interval` auto-calc (`interval.ts`) + a `zustand` time store with a per-tick frozen `now` (`store.ts`); a `TimeRangePicker` (quick ranges + relative/absolute tabs, validated) + `RefreshControl` (off/5s…15m + manual) mounted in `DashboardToolbar`; an auto-refresh loop that pauses on a hidden tab + refires on return (`useAutoRefresh`); URL state `?from=&to=&refresh=` restore/reflect for shareable links (`useTimeUrlSync`/`urlState.ts`); and the 🔶 `useWidgetQuery` append that resolves the range against the frozen tick, sends `time_range`+`interval_secs`, and folds `tick`+interval into the TanStack key (C3 cache snapping). Mirrored tests: resolver, interval, store, urlState round-trip, quick-range integrity (165 pass total). pnpm typecheck/test/build green; no Rust touched so cargo gate unchanged. Committed only WS-01 hunks. Deferred zoom-by-drag (acceptance item 6) + per-panel override (item 7, "stretch") — logged in TODOs.md. C6 audit/undo N/A (WS-01 adds no persisted mutable kind — state lives in URL + dashboard model).
2026-06-09 14:21 — Wake complete: WS-01 subagent returned Done within this wake; row ✅ (8e5a2cdf). UI-only (backend passthrough pre-shipped by WS-03); pnpm typecheck/test(165)/build green. Block on WS-08 still honored. One WS spawned this firing. Next pending → WS-02 (Variables & Templating) on the following wake.
2026-06-09 14:40 — WS-01 ✅, WS-08 ⛔ (block honored, unresolved), WS-03/04/07/10 ✅, no WS 🔵, no STOP. Picked first pending in queue order → spawning WS-02 (Variables & Templating) on nexus-gaps; dep WS-03 macros committed.
2026-06-09 14:41 — Wake complete: WS-02 subagent returned Done within this wake; row ✅ (db0cee76). Variables table (0701, RLS) + CRUD routes + query-kind cascade with cycle detection (via WS-03 binder, injection-safe) + variable bar/editor + `?var-<name>=` URL round-trip folded into useWidgetQuery (C3). cargo build/test + pnpm typecheck/test(197)/build green. Committed only WS-02 hunks. Repeat-by-variable render deferred to WS-05; C6 audit/undo deferred. One WS spawned this firing. Next pending → WS-11 (Units & datetime prefs) on the following wake.
2026-06-09 15:10 — WS-02 ✅, WS-08 ⛔ (block honored, unresolved), WS-01/03/04/07/10 ✅, no WS 🔵, no STOP. Picked first pending in queue order → spawning WS-11 (Units & datetime prefs) on nexus-gaps; deps WS-04 + WS-10 committed.
2026-06-09 15:35 — Wake complete: WS-11 subagent returned Done within this wake; commits verified on nexus-gaps (9094aea9 + 146f9e85), row ✅. Backend prefs vertical: migration 1501 PG prefs store + route-pinned GET/PATCH /me/preferences + Accept-Units layer mounted + DTOs/openapi/TS regenerated; cargo build + 6 patch tests green, pnpm typecheck/test(197)/build green. Corrected stale WS-11 spec claim (PgPrefsStore ships, not sqlite-only). Series quantity-tagging + UI prefs screen + alerting unit-coupling deferred to follow-ups (TODOs.md). One WS spawned this firing. Next pending → WS-12 (Audit log + undo/redo) on the following wake.
2026-06-09 22:40 — WS-11 ✅ (9094aea9). Re-verify found the spec's "starter-prefs Postgres store deferred (sqlite-only)" claim STALE — `PgPrefsStore` ships + is tested; fixed WS-11_UNITS_AND_PREFS.md and bumped its Verified line. Landed the backend units/prefs vertical: migration 1501_prefs.sql (org+user tables, granted to nexus_runtime), `PgPrefsStore` + `SystemDefaults` mounted on AppState, a `PrefsResolverFor` adapter feeding the existing `Accept-Units`/`UnitsCtx` layer (mounted on the product router inside the principal layer), and nexus-owned `GET`/`PATCH /api/v1/me/preferences` pinning `workspace_id = principal.tenant_id` (route-pinned isolation — the store runs outside `tenant_tx`, so no `app.tenant_id` GUC; the starter `?org=` selector is not exposed). DTO-first: `ResolvedPreferences`/`PreferencesPatch` registered, openapi.json + TS client regenerated. Tests: 6 patch-merge unit tests pass; 4 docker-gated route e2e cases (defaults/patch-roundtrip/cross-tenant-isolation/no-tenant-401) compile. cargo build --workspace green; pnpm codegen/typecheck/test(197)/build green. Series quantity-tagging at the query edge + UI prefs screen + alerting render-in-recipient-units deferred as follow-ups (TODOs.md, WS-11.md). Pre-existing out-of-lane break noted: docker-gated grant_gate_test.rs fails to compile (NewDashboard gained icon/accent from WS-01) — left for that owner. Committed only WS-11 hunks. Next pending → WS-12 (Audit log + undo/redo).
2026-06-09 15:40 — WS-11 ✅ (gated prior wake), WS-08 ⛔ (block honored, unresolved), WS-01/02/03/04/07/10 ✅, no WS 🔵, no STOP. Picked first pending in queue order → spawning WS-12 (Audit log + undo/redo) on nexus-gaps; changelog substrate, no committed-dep blockers.
2026-06-09 15:40 — Wake complete: WS-12 subagent returned Done within this wake; commits verified on nexus-gaps (224027ec + fdad8356), WS-12.md Status `Done`, row ✅. Tenant-scoped+RLS changelog substrate (migrations 1601–1603): recorder/log/registry, dashboard+datasource Reversibles + coverage guard, audit/undo/redo + GDPR-forget routes, hourly retention prune sweep, Cmd+Z undo/redo UI + Audit screen + before→after diff + per-resource History tab. cargo lib(70) + audit-e2e(7, docker) + pnpm typecheck/test(202)/build GREEN. delete-inverse (needs id-stable insert in WS-05/WS-08 stores) + pre-existing grant_gate drift → TODOs.md. One WS spawned this firing. Next pending → WS-05 (Folders/Rows/Repeat/JSON/Versioning) on the following wake.
2026-06-09 16:31 — WS-12 ✅ (gated prior wake), WS-08 ⛔ (block honored, unresolved), WS-01/02/03/04/07/10/11 ✅, no WS 🔵, no STOP. Picked first pending in queue order → spawning WS-05 (Folders/Rows/Repeat/JSON/Versioning) on nexus-gaps; dep WS-02 (Variables) committed.
2026-06-09 — WS-05 ✅. Shipped the self-contained, fully-correct slices end-to-end: (1) folders — migration 0602 (nestable, RLS, re-root-on-delete) + store (`insert`/`insert_with_id`/`by_id`/`list`/`update`/`delete`) + DTOs + CRUD routes with C6 recording + a snapshot-strategy folder `Reversible` (registered, on the coverage manifest) + the UI data layer (`api/folders/*`, `useFolders`, `useFolderMutations`); (2) `folder_id`+`starred` on dashboards — migration 0603 + store/record/DTO/route plumbing (three-valued `folder_id` patch via `clear_folder`, `starred` toggle); (3) `insert_with_id` on the dashboard + folder stores, resolving the WS-12 delete-undo follow-up so undo-of-delete / redo-of-create restore the original id; (4) `POST /dashboards/:slug/duplicate` (copies dashboard + panels + variables under a fresh id/derived slug); (5) C1 JSON model — `GET /dashboards/:slug/export` + `POST /dashboards/import` (validates `schema_version`, drops an absent datasource to NULL per the cross-tenant rule); (6) C6 `record(...)` wired into dashboard create/update/delete. DTO-first: openapi.json regenerated (nexus-openapi bin) + pnpm codegen; folder/export hooks added. `cargo test --workspace --lib` (73) + folder reversible round-trip unit tests + UI typecheck/test(202)/build all GREEN. Deferred (TODOs.md): collapsible rows, repeat-by-variable render, public/snapshot/embed sharing, version-checkpoint UI, and the folder-tree/star/export component UI (hooks shipped, component wiring deferred to avoid half-landing). Pre-existing out-of-lane break flagged: `alert_crud` test constructs `NewRule` without its required fields (alerts WS drift, no alert source touched here). Committed only WS-05 hunks. Next pending → WS-06 (Flows Visual Builder).
